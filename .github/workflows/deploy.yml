name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  test-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "GitHub Actions is working successfully!"